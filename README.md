## Health &amp; diet tracker with camera food capture, calorie logging, summaries, graphs, and in-app helper chat

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
